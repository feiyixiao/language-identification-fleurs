# Core ML — torch 2.5.x required (transformers 5.x needs ≥2.6)
torch==2.5.1
torchaudio==2.5.1
numpy<2  # datasets 2.x is not compatible with numpy 2.x

# HuggingFace stack (pinned for torch 2.5.1 compatibility)
transformers==4.44.2
datasets==2.20.0
evaluate>=0.4.0
accelerate>=0.27.0
huggingface_hub>=0.22.0

# Baselines
scikit-learn>=1.3.0

# Visualisation
matplotlib>=3.7.0
seaborn>=0.12.0

# Audio decoding (soundfile used as backend; torchcodec NOT supported with torch 2.5.x)
soundfile>=0.12.0
librosa>=0.10.0
